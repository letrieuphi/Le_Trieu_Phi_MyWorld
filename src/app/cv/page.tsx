import type { Metadata } from 'next';
import { PageHeading } from '@/components/layout';
import { profile, skills, tools } from '@/content/profile';
import { experience } from '@/content/experience';
import { asset } from '@/lib/site';
export const metadata: Metadata = { title: 'CV', description: 'Experience, education and creative practice of Lê Triệu Phi.' };
export default function CV() { return <section className="section cv-page"><PageHeading label="EXPERIENCE / EDUCATION / PRACTICE" title="Lê Triệu Phi" text={profile.title} /><div className="cv-downloads">{(['vi', 'en'] as const).map(lang => profile.cv[lang] ? <a key={lang} className="pill-link" href={asset(profile.cv[lang])} download>Download CV — {lang === 'vi' ? 'Vietnamese' : 'English'} ↓</a> : <button key={lang} className="pill-link" disabled>Download CV — {lang === 'vi' ? 'Vietnamese' : 'English'} <span>Coming soon</span></button>)}</div><div className="cv-layout"><aside><section><h2>Education</h2><p>Van Lang University<br />Digital Art & Design</p><span>Third-year student · K30</span></section><section><h2>Languages</h2><p>Vietnamese — Native<br />English — VSTEP B2</p></section><section><h2>Contact</h2><a href={`mailto:${profile.email}`}>{profile.email}</a></section></aside><div><h2 className="cv-section-title">Experience</h2>{experience.map(e => <section className="cv-experience" key={e.company}><span className="eyebrow">{e.dates}</span><h3>{e.role}</h3><h4>{e.company}</h4><p>{e.detail}</p></section>)}<h2 className="cv-section-title">Skills</h2><p className="skill-list">{skills.join(' · ')}</p><h2 className="cv-section-title">Tools</h2><div className="tool-grid">{Object.entries(tools).map(([group, list]) => <div key={group}><h3>{group}</h3><p>{list.join(' / ')}</p></div>)}</div></div></div></section>; }
