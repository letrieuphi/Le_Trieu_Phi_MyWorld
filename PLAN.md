# Implementation plan

1. Inspect: repository cloned and confirmed empty. Preserve the requested repository name.
2. Architecture: Next.js App Router, TypeScript, plain CSS; static export, optional base path. Shared project/category route from typed content. CSS perspective for lightweight 3D without WebGL overhead.
3. Visual system: charcoal, ivory, warm orange; editorial typography, aperture sculpture, restrained motion. Original abstract placeholders explicitly labeled, never passed off as project photography.
4. Build home, work hub, six categories, reusable case studies, about, CV and contact. Optional data hidden when absent.
5. Verify lint, typecheck, production build; desktop/mobile browser interactions, intro/session, keyboard menu and reduced motion. Check exported links.
6. Commit and publish to the authorized GitHub repository. Configure a static deployment if account access permits; Vercel remains supported.

Missing real media and CV files are content dependencies, tracked in CONTENT_TODO.md.
